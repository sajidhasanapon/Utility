
1.	The directory containing run_python.bat must be included in the path variables.
	(Alternatively, the full path has to mentioned in the "Python.sublime-build" file).



2.	The default Python.sublime-build file needs to be updated with the
	Package Resource Viewer package.
	To install the package, open Package Control, select "install" and,
	search for "prv" (without the quotes).